import * as pulumi from "@pulumi/pulumi";
/**
 * Secrets related to a Machine Learning compute. Might differ for every type of compute.
 */
export declare function listComputeKeys(args: ListComputeKeysArgs, opts?: pulumi.InvokeOptions): Promise<ListComputeKeysResult>;
export interface ListComputeKeysArgs {
    /**
     * Name of the Azure Machine Learning compute.
     */
    readonly computeName: string;
    /**
     * The name of the resource group. The name is case insensitive.
     */
    readonly resourceGroupName: string;
    /**
     * Name of Azure Machine Learning workspace.
     */
    readonly workspaceName: string;
}
/**
 * Secrets related to a Machine Learning compute. Might differ for every type of compute.
 */
export interface ListComputeKeysResult {
    /**
     * The type of compute
     */
    readonly computeType: string;
}
