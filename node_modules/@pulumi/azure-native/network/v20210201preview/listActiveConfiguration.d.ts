import * as pulumi from "@pulumi/pulumi";
import { output as outputs } from "../../types";
/**
 * Result of the request to list active configurations. It contains a list of active configurations and a URL link to get the next set of results.
 */
export declare function listActiveConfiguration(args: ListActiveConfigurationArgs, opts?: pulumi.InvokeOptions): Promise<ListActiveConfigurationResult>;
export interface ListActiveConfigurationArgs {
    /**
     * The name of the network manager.
     */
    readonly networkManagerName: string;
    /**
     * List of regions.
     */
    readonly regions?: string[];
    /**
     * The name of the resource group.
     */
    readonly resourceGroupName: string;
    /**
     * When present, the value can be passed to a subsequent query call (together with the same query and scopes used in the current request) to retrieve the next page of data.
     */
    readonly skipToken?: string;
}
/**
 * Result of the request to list active configurations. It contains a list of active configurations and a URL link to get the next set of results.
 */
export interface ListActiveConfigurationResult {
    /**
     * When present, the value can be passed to a subsequent query call (together with the same query and scopes used in the current request) to retrieve the next page of data.
     */
    readonly skipToken?: string;
    /**
     * Gets a page of active configurations.
     */
    readonly value?: outputs.network.v20210201preview.ActiveConfigurationResponse[];
}
